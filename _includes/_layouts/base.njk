<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0";http-equiv="Content-Security-Policy" content="default-src 'none'">
    
    {# De 'title' variabele komt uit de frontmatter van de specifieke pagina die deze layout gebruikt. #}
    {# Als 'title' niet is opgegeven in de frontmatter, wordt "Mijn Recepten" gebruikt. #}
    <title>{{ title or "Mijn Recepten" }} - Receptenwebsite</title>
    
    {# Link naar je CSS bestand. De `url` filter zorgt voor een correct pad vanaf de root van de site. #}
    <link rel="stylesheet" href="{{ '/assets/css/style.css' | url }}">
    
    {# Google Fonts - Merriweather (zoals in je style.css) #}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Merriweather:ital,wght@0,300;0,400;0,700;0,900;1,300;1,400;1,700;1,900&display=swap" rel="stylesheet">

    {# Voeg hier eventueel andere head-elementen toe, zoals favicons, etc. #}
</head>
<body>
    <header class="site-header">
        <div class="header-content">
            {# Link naar de homepage. Je kunt de class "site-title" gebruiken voor de styling. #}
            <a href="{{ '/' | url }}" id="homeLink" class="site-title">
                Recepten van R {# Of een andere algemene titel/logo #}
            </a>
            
            <nav class="site-nav">
                <ul>
                    <li><a href="{{ '/' | url }}">Home</a></li>
                    <li><a href="{{ '/koken/' | url }}">Koken</a></li>
                    <li><a href="{{ '/bakken/' | url }}">Bakken</a></li>
                    {# Voeg hier eventueel andere navigatielinks toe #}
                </ul>
            </nav>
        </div>
    </header>

    <main class="main-content">
        {# De specifieke content van elke pagina (die deze layout gebruikt) komt hier terecht. #}
        {{ content | safe }}
    </main>

    <footer class="site-footer">
        <p>&copy; 2024 Jouw Naam. Alle rechten voorbehouden.</p> {# Jaartal hardgecodeerd voor eenvoud #}
    </footer>

    {# Eventuele globale JavaScript-bestanden kunnen hier aan het einde van de body geladen worden.
       Bijvoorbeeld, als je de zoekfunctionaliteit van script.js op meerdere pagina's zou gebruiken:
       <script src="{{ '/assets/js/script.js' | url }}"></script> 
    #}
</body>
</html>