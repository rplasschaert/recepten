<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {# De titel komt uit de front matter van de specifieke pagina, met een fallback #}
    <title>{{ title | default("Mijn Receptenverzameling") }}</title>

    {# Link naar je hoofd stylesheet. Zorg dat style.css in 'assets/css/' staat #}
    <link rel="stylesheet" href="/assets/css/style.css">

    {# Link naar Google Fonts, als je die overal gebruikt #}
    <link href="https://fonts.googleapis.com/css2?family=Merriweather&display=swap" rel="stylesheet">

    {# Hier kun je later eventueel andere algemene head-elementen toevoegen #}
</head>
<body>

    <header class="site-header">
        {# Een algemene header voor de hele site #}
        {# Je kunt dit later uitbreiden met een volledige navigatiebalk #}
        <div class="header-content" style="padding: 15px; background-color: #333; color: #fff; text-align: center;">
            <a href="/" style="text-decoration: none; color: #fff;">
                {# Zorg dat home-icon.png in 'assets/images/' staat #}
                <img src="/assets/images/home-icon.png" alt="Homepage" style="width: 30px; height: 30px; vertical-align: middle; margin-right: 10px;">
                <span class="site-title" style="font-size: 1.5em;">{{ site.name | default("Mijn Receptenverzameling") }}</span>
            </a>
            {# Voorbeeld van een simpele navigatie als je de Eleventy Navigation Plugin gebruikt:
               (Voeg 'eleventy-plugin-navigation' toe aan je package.json en .eleventy.js)
            <nav>
                <ul>
                    {%- for entry in collections.all | eleventyNavigation %}
                    <li class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}">
                        <a href="{{ entry.url }}">{{ entry.key }}</a>
                    </li>
                    {%- endfor %}
                </ul>
            </nav>
            #}
        </div>
    </header>

    <div class="main-content-wrapper" style="padding: 20px; max-width: 900px; margin: 0 auto;">
        {# Hier wordt de specifieke content van elke pagina ingevoegd #}
        {{ content | safe }}
    </div>

    <footer class="site-footer" style="text-align: center; padding: 20px; margin-top: 40px; border-top: 1px solid #eee;">
        <p>&copy; {{ site.author | default("Jouw Naam") }}. Alle rechten voorbehouden.</p>
    </footer>

    {# Link naar je algemene JavaScript bestand. #}
    {# Zorg dat je wake-lock script (en andere globale scripts) in 'assets/js/script.js' staan #}
    <script src="/assets/js/script.js"></script>

</body>
</html>